# Requirements for Cloud Deployment (Minimal)
fastapi==0.110.0
uvicorn==0.27.1
supabase==2.28.0
python-dotenv
python-multipart==0.0.9
pillow
numpy==1.26.4
opencv-python-headless==4.9.0.80

# Note: Heavy simulators (qiskit) are excluded from cloud build 
# as they require system-level BLAS/LAPACK libraries.